namespace Scorm.Tests;

/// <summary>No slice exists yet — real tests land with Slice 2 (SCORM Launch &amp; Completion).</summary>
public class PlaceholderTests
{
    [Fact]
    public void Module_assembly_loads() => Assert.NotNull(typeof(LibreLms.Modules.Scorm.ModuleMarker));
}
