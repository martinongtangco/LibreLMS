@model LibreLms.Host.Pages.Admin.Organizations.OrgTreeNode

<div class="card mb-2" style="margin-left: @(Model.ParentId.HasValue ? "20px" : "0");">
    <div class="card-body py-2">
        <strong>@Model.Name</strong>
        @if (!string.IsNullOrEmpty(Model.Description))
        {
            <span class="text-muted"> — @Model.Description</span>
        }
        <div class="mt-1">
            <a asp-page="Edit" asp-route-id="@Model.Id" class="btn btn-sm btn-outline-primary">Edit</a>
            @if (!Model.ParentId.HasValue)
            {
                <span class="badge bg-secondary">Root</span>
            }
        </div>
    </div>
</div>

@if (Model.Children.Any())
{
    @foreach (var child in Model.Children)
    {
        <partial name="_OrgNode" model="child" />
    }
}
