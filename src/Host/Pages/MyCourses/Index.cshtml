@page

@model MyCoursesModel
@{
    ViewData["Title"] = "My Courses";
}

<h1 class="mb-lg">My Courses</h1>

<div class="mb-md">
    <button type="button"
            class="btn btn-ghost"
            hx-get="/MyCourses?handler=Enrollments"
            hx-target="#enrollment-list"
            hx-trigger="click">↻ Refresh</button>
</div>

<div id="enrollment-list">
    @await Html.PartialAsync("_EnrollmentList", Model.EnrollmentRows)
</div>
