Use-case logic for **Catalog** (plain application services — no CQRS/MediatR framework per
the constitution). Depends on Domain and on abstractions it defines itself;
Infrastructure implements those abstractions, never the other way around.
